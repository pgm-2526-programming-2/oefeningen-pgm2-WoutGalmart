/**
 * @module Utils
 */

/**
 * funciton is uppercase a given text
 * @param {string} str - The string to be converted to uppercase
 * @returns {string} The uppercase version of the input string
 */

function toUpperase(str) {
  return str.toUpperCase();
}
